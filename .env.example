# App runtime secrets (deploy platforms inject these as real env vars).
# Copy to .env.local for local dev; direnv loads it via .envrc.
OPENAI_API_KEY=your_key_here

# Dev-tooling tokens live in .env (never shipped), e.g.:
# DAISYUI_BLUEPRINT_LICENSE=...
# FIGMA_TOKEN=...
